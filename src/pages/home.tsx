import React from 'react';
import { View, StyleSheet } from 'react-native';

export const HomePage = () => {

  return (
    <View style={styles.container} />
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#50B9DE'
  }
});
